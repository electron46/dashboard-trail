Navigation par onglets pilule, repris de la charte graphique (nav du document de marque). En mono, actif = fond anthracite plein.

```jsx
<Tabs items={[{value:'designer',label:'Designer'},{value:'dev',label:'Développeur'}]} active={tab} onChange={setTab} />
```
