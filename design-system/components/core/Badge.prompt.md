Étiquette de statut courte, toujours en majuscules mono. `principal`/`secondaire` pour classer un objectif ou une échéance, `success`/`danger` pour un état système, `neutral` par défaut.

```jsx
<Badge tone="principal">Objectif principal</Badge>
<Badge tone="danger">Échec import</Badge>
```
