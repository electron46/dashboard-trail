Bascule pour un réglage binaire (ex. afficher les séances approximées, activer le retour IA par défaut).

```jsx
<Switch checked={aiEnabled} onChange={setAiEnabled} label="Retour IA automatique" />
```
